# =============================================================================
# .env.example — copy to .env and fill in values
# Never commit .env to git.
# =============================================================================

# HuggingFace token (needs read access to gated LLaMA models)
HF_TOKEN=hf_...

# Modal token (set via `modal setup` normally, but can override here)
# MODAL_TOKEN_ID=ak-yAOIJNSDEpCfXYuEEAT0GM
# MODAL_TOKEN_SECRET=as-WVr7UjSPa8zStHPz19eSXM

# Optional: WandB for experiment tracking
# WANDB_API_KEY=...
# WANDB_PROJECT=drdpo_repro
# WANDB_ENTITY=your_username
